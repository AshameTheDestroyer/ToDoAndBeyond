@using ToDoAndBeyond.Enums
@code
{
    [Parameter]
    public required EmptyMessageType EmptyMessageType { get; set; }
}

<div class="empty-message">
    <span>🥲</span>
    <p>There's no <strong>@(EmptyMessageType + "s")</strong> here yet.</p>
</div>